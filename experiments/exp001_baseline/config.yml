# experiment/config.yaml
model:
  name: efficientnet_b3
  pretrained: true

training:
  epochs: 25
  batch_size: 64
  lr: 3e-5
  optimizer: AdamW

data:
  img_size: 380
  augmentations: strong
  val_split: 0.15